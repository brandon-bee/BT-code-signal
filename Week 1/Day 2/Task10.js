// write a function that returns the division of two numbers (numerator, denominator)
// if the denominator is 0, return 0

function division(numerator, denominator) {
    if (denominator === 0) {
        return 0;
    }
    return numerator / denominator;
}