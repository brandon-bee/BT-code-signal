// write a function that takes two parameters, birth_year and current_year
// create a diff variable that stores the difference between the two years
// return the birth_year minus the diff

function solution(birth_year, current_year) {
  let diff = current_year - birth_year;
  return birth_year - diff;
}